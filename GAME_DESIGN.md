# 監督ハクスラ MVP設計メモ

## 目的

「野球×ハクスラ×監督シミュレーション」を、1年間のサイクルが最後まで遊べる縦切りMVPとして実装する。実在球団、実在選手、既存ゲーム由来の名称や素材は使わず、架空12球団と架空選手だけで構成する。

## MVPで採用した仮仕様

- 12球団を東都リーグ、西都リーグの2リーグに6球団ずつ配置する。
- 新規ゲーム初年度はドラフトから開始する。2年目以降は表彰、加齢、オフシーズン行動、ドラフト、通常8ラウンド、ポストシーズンの順で進む。
- 通常ラウンドは8回、1ラウンド3試合。数値は `CONFIG.season` に集約する。
- プレイヤー球団はNPCよりやや有利な初期戦力にする。プレイヤーの強さに応じたラバーバンド補正は入れない。
- 試合は打席単位の簡易確率モデルで進行する。試合の途中に1回だけ重要場面が発生し、装備中の作戦または静観を選ぶ。
- 確率は選手能力、調子、疲労、練習方針、作戦、相手戦力から計算する。結果を先に決めて演出する方式にはしない。
- ドラフト候補は現在能力を概ね正確に表示し、潜在能力と故障傾向はスカウト精度に応じた範囲表示にする。
- AI球団はプレイヤー指名後に候補を1人指名し、候補リストから消える。
- シーズン途中の報酬は、軽い報酬、強い報酬、最終ラウンド報酬、ポストシーズン報酬の4段階に分ける。
- ポストシーズンはリーグ3位以内でCS進出。CSは2勝先取、日本シリーズは4勝先取。
- 日本シリーズ優勝で完全クリアとし、ノーマル初優勝なら名将ポイント3点とハード条件を解放する。
- ハード条件の合計値が過去最高を超えて日本一になった時だけ、未獲得の閾値報酬をまとめて付与する。
- オフシーズン行動は3回。同じ行動は複数回選べるが、同一年内の2回目以降は効果が低下する。
- 消費アイテム所持枠は5個。衰え巻き戻し系アイテムは同じ選手に複数回使用できない。
- 名将ポイントは候補数、初期資金、作戦枠、練習枠、アイテム解放に振り直せる。
- セーブは `localStorage` に自動保存する。JSONの書き出し、読み込みに対応し、`schemaVersion` と乱数状態を保存する。

## 2026-09 試遊感想に基づく改修仕様

- 試合は4回まで自動進行し、5回以降に最大3回の介入機会を作る。介入回数は `CONFIG.match.interventionOpportunities`、開始イニングは `CONFIG.match.interventionStartInning` で調整する。
- 作戦は試合終了まで持続しない。`duration: "plateAppearance"` は次の1打席のみ、`duration: "halfInning"` は現在の攻撃側または守備側の半イニング終了まで有効とする。
- 作戦は `side: "offense"` または `side: "defense"` で攻守を制限する。走者や終盤条件は `condition` で管理し、UI表示だけでなくエンジン側でも不正使用を拒否する。
- 作戦選択後は半イニング結果を確認する `result` 状態を挟む。試合終了後も即座に報酬へ移らず、`final` 状態で最終スコア、主要成績、作戦ログを確認してから確定する。
- 走者状況は `bases` の真偽配列で保存し、画面では文字と簡易ダイヤモンドの両方で表示する。
- 既存セーブは `schemaVersion: 1` から読み込み時に `schemaVersion: 2` へ移行する。成長基準がない選手は現在値を「記録開始時点」として保存し、過去の成長を捏造しない。
- 選手詳細は年度開始時点または加入時点の能力を `seasonBaseline` として持ち、今年度の増減を表示する。
- 通常の装備画面には所持済みの作戦・練習方針だけを表示する。試合中は閲覧のみで、装備変更はエンジン側でも拒否する。
- ドラフト候補は生成時に能力推定範囲を固定し、詳細表示を開き直しても範囲が変わらない。投手と野手で紹介文の評価軸を分ける。
- 報酬候補は同一画面で同じ用途の単純下位互換が並びにくいよう、タイプと対象の重複を避けて生成する。重複装備は素材化する。

## 後回しにした要素

- トレード、FA、契約更改、年俸、複雑なフロント交渉。
- 球種別能力、球場補正、左右相性の詳細、守備位置ごとの厳密な適性。
- 一球単位の配球、盗塁、バント、代走、詳細な継投管理。
- wRC+、WAR、FIPなど高度な指標。追加できるよう、成績集計は `stats.js` に分離する。
- 故障による長期離脱、治療期間、リハビリ工程。
- 監督能力の持ち込み枠の厳密管理。MVPでは名将ポイントによる永続パークとして扱う。
- 大量の装備・選手名・イベント。MVPはデータ追加で増やせる最小構成に留める。

## 主要調整値

- `CONFIG.season.rounds`: 通常ラウンド数。初期値8。
- `CONFIG.season.gamesPerRound`: 1ラウンドの試合数。初期値3。
- `CONFIG.match.managerPoints`: 1試合の監督ポイント。初期値3。
- `CONFIG.draft.maxPlayerPicks`: ドラフト獲得上限。初期値3。
- `CONFIG.equipment.tacticSlots` / `trainingSlots`: 作戦、練習の基本装備枠。初期値3。
- `CONFIG.rewards.rarityWeights`: 報酬レアリティ抽選。
- `CONFIG.legacy.hardRewards`: ハード合計値に対する一度限りの名将ポイント報酬。

## データ拡張方針

球団、作戦、練習方針、消費アイテム、監督パーク、ハード条件は `src/game/data.js` に定義する。数値バランスは `src/game/config.js` に寄せ、UIや状態遷移のコードへ直接埋め込まない。
